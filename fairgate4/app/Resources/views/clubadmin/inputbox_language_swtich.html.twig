                        <div class="row">
                            <div class="col-md-12">
                               <div class="col-md-6 col-sm-6 col-xs-12 fg-left-zero"> {% for clubLang in clubLanguages %}
                                   <input type="text" data-key="{{result_data.id}}.i18n.{{clubLang}}.title" data-filtertype="role" name="{{result_data.id}}_i18n_{{clubLang}}_title" id="{{result_data.id}}_i18n_{{clubLang}}_title" data-lang="{{clubLang}}" class="form-control input-lg page-title {{(clubLang == clubDefaultLang) ? '' : 'hide'}}" placeholder="{{placeholder|default('ROLE_CATEGORY_PLACEHOLDER')|trans}}"  value="{{(result_data['titleLang'][clubLang] is defined) ? result_data['titleLang'][clubLang] : result_data.title}}" required="true">
                                {% endfor %}</div>
                               <div class="btn-toolbar fg-lang-top-btm pull-left">
                                    <div class="btn-group fg-br-lang-bar">
                                        {% if (clubLanguages|length > 1) %}
                                            {% for clubLang in clubLanguages %}
                                                <button type="button" class="btn {{(clubLang == clubDefaultLang) ? 'adminbtn-ash' : 'fg-lang-switch-btn'}}" data-selected-lang="{{clubLang}}" data-elem-function="switch_lang">{{clubLang|upper}}</button>
                                            {% endfor %}
                                        {% endif %}
                                    </div>
                                </div>
                                </div>
                            </div>
                  