Modified select box option selector to handle on change selectbox event when <option> within <optgroup> on 27-05-2015 in 3 places
Changed "$object.children("option").each(function(o)" to "$object.find("option").each(function(o)"